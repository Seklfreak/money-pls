{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "No rate limiting: updates are opened as soon as they are found. The defaults (2 an hour, 10 at once) starve low-churn dependencies behind high-churn ones - gitea/act_runner sat four minor versions behind for months that way.",
  "prHourlyLimit": 0,
  "prConcurrentLimit": 0,
  "branchConcurrentLimit": 0,
  "extends": [
    "config:recommended",
    "group:allNonMajor"
  ],
  "assignees": [
    "Seklfreak"
  ],
  "packageRules": [
    {
      "description": "Auto-merge non-major updates once the test workflow passes.",
      "matchUpdateTypes": [
        "minor",
        "patch",
        "pin",
        "digest"
      ],
      "automerge": true
    },
    {
      "description": "Major updates: open a PR but require manual review/merge.",
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": false
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "SwiftLint and XcodeGen are downloaded from their GitHub releases in the workflow, so no built-in manager sees the pinned versions. Reads the `# renovate:` annotation above each env var, so a third pinned tool needs the annotation and nothing here.",
      "managerFilePatterns": [
        "/(^|/)\\.github/workflows/.*\\.ya?ml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>\\S+)\\s+[A-Z0-9_]+: \"(?<currentValue>[^\"]+)\""
      ],
      "versioningTemplate": "semver"
    }
  ]
}
